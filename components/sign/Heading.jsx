import React from "react";

const Heading = ({ label }) => {
  return (
    <div className="text-center font-notoDis text-[4.2vw] font-medium tracking-tighter">
      {label}
    </div>
  );
};

export default Heading;
